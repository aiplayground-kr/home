import { seasonOneEvents } from "../../content";
import { PageIntro, SiteFooter, SiteHeader } from "../../site-shell";

export default function SeasonOnePage() { return <main><SiteHeader />
  <PageIntro eyebrow="SEASON 1 · 2024—2026" title="시즌 1 현장 게시판" description="네 번의 만남에서 오간 질문과 배움을 실제 포스터와 함께 생생하게 펼쳐봅니다." />
  <section className="archive-board section-pad compact">{seasonOneEvents.map(e => <article id={e.slug} key={e.slug}><div className="archive-poster"><img src={e.image} alt={`${e.title} 공식 포스터`} /><span>{e.label}</span></div><div className="archive-copy"><div className="post-meta"><span>POST {e.number}</span><time>{e.date}</time></div><h2>{e.title}</h2><p>{e.summary}</p><div className="tags">{e.tags.map(t => <span key={t}>#{t}</span>)}</div><div className="field-note"><span>LINKEDIN FIELD NOTE</span><h3>현장 결과</h3><ul>{e.results.map(result => <li key={result}>{result}</li>)}</ul><h3>참여자·연사 피드백</h3><blockquote>{e.feedback}</blockquote><div className="source-links">{e.sources.map(source => <a href={source.url} target="_blank" rel="noreferrer" key={source.url}>{source.label}</a>)}</div></div><details><summary>행사 개요 다시 보기</summary><p>{e.detail}</p></details></div></article>)}</section><SiteFooter /></main> }
